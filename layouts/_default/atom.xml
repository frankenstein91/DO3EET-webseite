<feed xmlns="http://www.w3.org/2005/Atom">
  <title>{{ if eq .Title .Site.Title }}{{ .Site.Title }}{{ else }}{{ with .Title }}{{.}} on {{ end }}{{ .Site.Title }}{{ end }}</title>
  <link href="{{ .Permalink }}atom.xml" rel="self"/>
  <link href="{{ .Permalink }}"/>
  <updated>{{ now.Format "2006-01-02T15:04:05Z07:00" | safeHTML }}</updated>
  <id>{{ .Permalink }}</id>
  <author>
    <name>{{ .Site.Author.name }}</name>
  </author>
  <generator uri="https://gohugo.io/" version="{{ hugo.Version }}">Hugo</generator>
  {{ range .RegularPages }}
  <entry>
    <title>{{ .Title }}</title>
    <link href="{{ .Permalink }}"/>
    <id>{{ .Permalink }}</id>
    <updated>{{ .Lastmod.Format "2006-01-02T15:04:05Z07:00" | safeHTML }}</updated>
    <published>{{ .Date.Format "2006-01-02T15:04:05Z07:00" | safeHTML }}</published>
    <content type="html">
      {{- printf "<![CDATA[" | safeHTML -}}
      {{- $content := .Content -}}
      {{- /* 1. Barrierefreiheits-Texte (sr-only) entfernen */ -}}
      {{- $content = $content | replaceRE "<span class=\"sr-only\">.*?</span>" "" -}}
      {{- /* 2. Heading Hashtags (##) entfernen */ -}}
      {{- $content = $content | replaceRE "<span class=\"heading-hashtag\" aria-hidden=\"true\">.*?</span>" "" -}}
      {{- /* 3. Design-Klassen (linux-egg) säubern */ -}}
      {{- $content = $content | replaceRE "<span class=\"linux-egg\">(.*?)</span>" "$1" -}}
      {{- /* 4. Whitespace aufräumen */ -}}
      {{- $content = $content | replaceRE "[\n\r]+" "\n" -}}
      {{- $content | safeHTML -}}
      {{- printf "]]>" | safeHTML -}}
    </content>
  </entry>
  {{ end }}
</feed>
